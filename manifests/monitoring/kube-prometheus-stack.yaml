spec:
  interval: 15m
  chart:
    spec:
      chart: kube-prometheus-stack
      version: "85.x"
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
  install:
    createNamespace: true
    crds: CreateReplace
    disableWait: true        # ← Add this
  upgrade:
    crds: CreateReplace
    disableWait: true        # ← Add this
  values:
    prometheus:
      service:
        type: NodePort
        nodePort: 30090
      prometheusSpec:
        retention: 15d
        resources:
          requests:
            cpu: 200m
            memory: 200Mi
    prometheus-node-exporter:
      enabled: false
      hostPort: false
    grafana:
      enabled: false
      sidecar:
        dashboards:
          enabled: true
          label: grafana_dashboard
          labelValue: "1"
          searchNamespace: ALL